{
  "scripts": {
    "build": "npx tailwindcss@3 -i ./input.css -o ./dist/output.css && rsync -a --exclude='dist/' ./ dist/",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.0.3",
    "gh-pages": "^6.2.0"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
